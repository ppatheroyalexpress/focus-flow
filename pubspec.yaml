name: focus_flow
description: Focus Flow - A premium Pomodoro Timer & Task Manager for deep work.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  audioplayers: ^5.2.1
  flutter_local_notifications: ^16.3.2
  timezone: ^0.9.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.5
  share_plus: ^9.0.0
  path_provider: ^2.1.3
  file_picker: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
